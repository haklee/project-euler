# explanation

Build a trie with reversed binary prime numbers. Manage total leaf count each child is holding while building. Add `max(children leaf count)` to the solution by traversing the trie tree with dfs.
