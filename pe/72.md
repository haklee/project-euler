# explanation

By observation, we can see that number of proper fraction where denominator is `d` equals to number of natural numbers less than `d` where `gcd(n, d) = 1`, which is same as the definition of the euler phi function.
