# explanation

For given integer sequence a:

```py
# get maximum-sum subsequence, O(n)
sol = 0
s = 0
for i in a:
    s+=i
    if s < 0:
        s = 0
    sol = max(s, sol)
```

Apply this method to all horizontal, vertical, diagonal or anti-diagonal sequences found in 2000 by 2000 array.
