# explanation

Brute force all the digit splitting combinations. We can get all the combinations by following method:

If we are given a number `abcde`, then we can decide putting `+` sign between any digits. This means, in `a^b^c^d^e`, for all `^` signs, we can decide if we put `+` or not.
