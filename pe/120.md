# explanation

By investigation, we can see that `((a+1)^n + (a-1)^n) % a^2`  equals to:

- for even number `n`, `(-n*a + 1) + (n*a + 1) == 2 (mod a^2)`
- for odd number `n`, `(n*a + 1) + (n*a - 1) == 2*n*a (mod a^2)`

Since we only need to find rmax for 3<=n<=1000, brute force to find answer.
